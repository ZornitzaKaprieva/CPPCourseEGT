#pragma once
#include"Game2.h"

class Shapes
{
public:
	static void drawCircle(SDL_Renderer* renderer, int centerX, int centerY, int radius); //can be called directly with Shape:: // can be called without an object
};

